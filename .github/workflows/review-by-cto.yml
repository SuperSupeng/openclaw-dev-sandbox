name: CTO Code Review
on:
  pull_request:
    types: [labeled]
jobs:
  review:
    if: github.event.label.name == 'cto-review'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: echo "CTO Review triggered for PR ${{ github.event.pull_request.number }}"
